import { FilterByLandingPipe } from './filter-by-landing.pipe';

describe('FilterByLandingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByLandingPipe();
    expect(pipe).toBeTruthy();
  });
});
